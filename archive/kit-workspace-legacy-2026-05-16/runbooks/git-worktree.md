# Git Worktree Runbook

Use worktrees only when the user wants isolated parallel work.

Before creating one:

- confirm target branch/name
- confirm parent directory
- check for existing uncommitted work
- avoid destructive cleanup

After use, remove worktrees only with explicit confirmation.
