# Dependency Upgrade Review

Use when package versions, lockfiles, runtime versions, base images, or SDK versions change.

Use `_workspace/runbooks/dependency-upgrade-safety.md`.

Check release notes, breaking changes, transitive dependency changes, install scripts, security advisories, license compatibility, runtime compatibility, and rollback path.

Do not install dependencies unless explicitly approved.
