# Database Migration Review

Use when reviewing schema migrations, data migrations, indexes, backfills, or ORM model changes.

Use `_workspace/runbooks/database-migration-safety.md`.

Check backward compatibility, locking, index strategy, data volume, backfill/retry behavior, rollback, app-version compatibility, and validation queries.

Ask before applying or running irreversible data operations.
