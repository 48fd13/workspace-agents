# Containerization Review

Use for Dockerfiles, Compose files, image build scripts, container runtime config, and base image changes.

Use `_workspace/runbooks/docker-container-safety.md`.

Check base image trust, pinned versions, multi-stage builds, build context, secrets in layers, non-root user, ports/envs, healthchecks, image size, and reproducibility.
