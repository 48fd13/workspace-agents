# Kubernetes Manifest Review

Use for Kubernetes YAML, Helm values/templates, Kustomize overlays, and deployment config.

Use `_workspace/runbooks/kubernetes-manifest-checklist.md`.

Check resources, probes, env/config/secrets, labels/selectors, service/ingress, rollout strategy, security context, namespaces, RBAC, and storage assumptions.

Do not mutate clusters without explicit approval.
