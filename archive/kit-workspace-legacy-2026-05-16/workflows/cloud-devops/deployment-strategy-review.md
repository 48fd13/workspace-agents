# Deployment Strategy Review

Use when choosing or reviewing how a backend/cloud service change is released.

Use `_workspace/runbooks/deployment-strategy-checklist.md`.

Check deployment type, rollout order, migrations/config coupling, feature flags, health checks, rollback, validation, and compatibility between old/new versions.

This is not an incident workflow; it is for normal release/change planning.
