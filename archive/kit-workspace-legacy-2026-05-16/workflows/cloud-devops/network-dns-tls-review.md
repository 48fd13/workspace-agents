# Network / DNS / TLS Review

Use for DNS records, certificates, load balancers, ingress, firewalls, security groups, routing, VPC/VNet/subnet, CDN, and public exposure changes.

Use `_workspace/runbooks/network-dns-tls-checklist.md`.

Check public/private exposure, routing, ports, protocols, TLS/cert lifecycle, DNS TTL, rollback, dependency impact, and validation commands.

Ask before changing live DNS, TLS, firewall, or routing state.
